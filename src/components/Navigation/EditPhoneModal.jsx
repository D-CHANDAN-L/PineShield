import PhoneEditModal from '../Layout/PhoneEditModal';

/**
 * Re-export PhoneEditModal to maintain backward compatibility
 * and ensure uniform automated background dispatch behavior.
 */
export default PhoneEditModal;
